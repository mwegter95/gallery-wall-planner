# Include Linux x64 optional deps in the lock file so that
# npm ci works in GitHub Actions (ubuntu-latest).
# Vite 8 uses rolldown which ships platform-specific native binaries.
supported-architectures-os[]=current
supported-architectures-os[]=linux
supported-architectures-cpu[]=current
supported-architectures-cpu[]=x64
